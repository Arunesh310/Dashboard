import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shadowfax.operationsdashboard',
  appName: 'Operations CCTV Dashboard',
  webDir: 'dist'
};

export default config;
