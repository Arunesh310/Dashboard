# Optional: shared CSV snapshot (Supabase). Copy to .env and fill in.
# Create a project at https://supabase.com, run supabase/schema.sql, then paste URL + anon key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
